<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
	<style name="guidebar_btn">
	    <item name="android:layout_width">@dimen/guidebar_btn_width</item>
        <item name="android:layout_height">@dimen/guidebar_btn_height</item>
        <item name="android:textSize">9sp</item>
        <item name="android:textColor">@color/guidebar_btn_text_color</item>
        <item name="android:singleLine">true</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">@dimen/guidebar_btn_paddingTop</item>
        <item name="android:paddingBottom">@dimen/guidebar_btn_paddingBottom</item>
	</style>
</resources>
